已知bug:

* 

```ai回复
"我没有把你这张参考图送进去。 工作流的 LoadImage（节点 89）读的是它自己被捕获时绑定的那张 jpg（478952ddd100baa1a543e7be0110b912c9fc2efa.jpg），我只能改文本，改不了这个文件名（改写会指向一个不存在的文件）。所以这次的产物是纯文生图 + 一个我无法控制的 LLLite 参考。如果你想要真正的 img2img / 结构参考，需要你在 ComfyUI 里把这张图放进 input 目录再跑一次让它被捕获。"
```

* 我上传文件，如果AI觉得有必要使用图生图，如何处理源图片呢？（同时你可能要适当修改skills，让它知道可以使用图生图）
* 生成产物后，在AI工作台界面的预览图不可用





建议：

* AI回复显示顺序：完全按照消息获取顺序来，不要将最终回复置于最终思考的前面；
* 思考过程默认自动折叠；
* 如果当前界面在最下方，信息流有新内容显示的时候自动滚动，否则保持在原位；
* 在对话主体页面右下角加一个回到聊天底部的按钮；
* 权限应该有询问/完全权限两档，在页面底部、附件按钮和模型选择中间提供选择，切换到完全权限的时候即时注入系统提示词，并且AI无需批准。

其他建议：



