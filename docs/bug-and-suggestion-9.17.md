已知bug:

* 点击切换允许，实际切换询问（没有实现完全权限吗？）
* 当前弹出菜单时会占住整个窗口的焦点，导致能滚动的页面无法滚动。
* 视频一直没有预览图
* ComfyUI 查询次数上限 3 次太少（投递附件 + 查节点 + 查工作流很容易撞上）→ **已改到 9 次**
  （2026-09-17，`ToolPolicyConfig.DEFAULT_MAX_COMFY_QUERIES_PER_RUN`；系统提示词同步为 v10，
  且读库时用 `normalizeStored` 覆盖掉库里冻着的旧值 3）
* AI 工作台会话列表里"次新那条"顶着一块亮灰底、看着像被选中（不是选中态）→ **已修**
  （那是 Material 的按下高亮 ~38% 白，窗口失去焦点/被最小化时会卡住；现在按下/悬停压到 10%/6%，
  当前会话改为明确底色 `primary@0.16`，`test/ai_home_test.dart` 有回归用例）



建议：

* “完全权限”不够准确，应该改成自动允许（无需批准）。当然，保留文件夹白名单设计

其他建议：

