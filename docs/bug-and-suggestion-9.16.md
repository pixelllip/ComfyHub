已知bug:
获取完可用模型并添加后，退出AI模型与凭据界面重新进入，模型全部消失不显示；
建议：
提前写好调用comfyUI的脚本，注册该工具供AI调用；
设置中AI模型与凭据选项应该放在靠前而不是靠后的位置；
实现markdown渲染；
其他建议：
视频渲染窗口大小应该尽量让长边铺满边界，而不是现在的16:9固定比例，并且加上全屏功能，视频预览图功能。
要求：
实现OpenAI responses接口（我稍后提供可用API）；
在编写代码前你查阅目前常见大模型的模态和思考强度，例如.dsh/settings.yaml就有现成的一些模型的模态信息。这样你导入模型就可能不用再查询models自带的信息。