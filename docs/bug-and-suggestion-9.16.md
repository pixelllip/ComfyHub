> 本清单（2026-09-16）已全部处理完毕，逐条对照见
> [`ai-home-progress-v0.1.md` 第 4.1~4.3 节](ai-home-progress-v0.1.md)。
> 下面保留原文，方便回溯。

已知bug:

* 视频播放页面右上角和右下角各有一个全屏按钮，点击全屏按钮后，视频从头播放并且无法按Esc退出。
* 删除数量大于单页画廊承载内容时，执行删除会导致提示画廊为空（即便其实没有删除所有内容）。刷新本页可恢复。

建议：

* 排查并解决滑动长列表界面卡顿的问题（比如AI模型与凭据界面）；
* 每次冷启动后，AI工作台界面应该是新建一个聊天记录；
* 如果聊天记录内容为空（下方对话框有文字或附件时除外），切换到别的聊天记录时可以删掉他；
* 记住我在对话中上一次使用的模型信息；
* AI工作台右上角的“上下文”和Comfy状态刷新按钮放在一起意义不明，要不删掉上下文字样，把刷新按钮放在Comfy字样的右边；

其他建议：

* 如果你要调试，应该使用flutter debug并能运用hot reload和hot restart，减少调试冷启动时间；
* 减少截屏和读取屏幕截图次数以降低token使用量；
* 能不能先访问%USERPROFILE%/.dsh/settings.yaml，提前登录这些已知模型的模态和思考强度信息，下次我获取model就不用手工配置了？

要求：

* 我希望画廊的图片/视频/音频被删除时，对应的提示词被显著标记“未关联”，批量管理提示词可以做到一键清除未关联产物的功能。

