已知bug:

* 打开ComfyHub的时候会产生大于1条新对话
* 在新对话/已有对话的输入框输入内容，切换对话后无法保存
* 快速滑动包括大量列表的窗口依旧卡顿（举例当前的AI模型与凭据界面，Command Code Goat有69个模型对应69个列表项，从页顶跳转到页底会卡顿）。先去网上调研有没有解决办法，也可使用Flutter DevTools分析原因。
* 右侧滚动条不能正确反映当前所处界面位置，举例当前的AI模型与凭据界面，从页顶往下滚的时候滚动条会变短。
* 目前markdown渲染还是有点不正常(表格/粗体)，详情可见我项目中最新一轮对话。

建议：

* 不应该有“从dsh导入”按钮。设置一个目录，让用户可以将skills拷贝到这个文件夹里，如果软件开启的时候检测到新的skill就自动注册。考虑release版的skills文件夹位置。

其他建议：

* 引入长期记忆（memory）。



要求：

